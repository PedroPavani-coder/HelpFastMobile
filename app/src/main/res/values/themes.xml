<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.HelpFastMobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Você pode customizar seu tema base aqui se precisar -->
    </style>

    <style name="Theme.HelpFastMobile" parent="Base.Theme.HelpFastMobile" />

    <style name="LabelStyle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="CustomInputStyle">
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">#A0A0A0</item>
        <item name="android:background">@drawable/background_edittext_dark</item>
        <item name="android:textSize">16sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <style name="FaqButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@android:color/white</item>
        <item name="android:textColor">#191932</item>
        <item name="android:textSize">14sp</item>

        <!-- CORRIGIDO: Substituído 'paddingVertical' por 'paddingTop' e 'paddingBottom' -->
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>

        <!-- CORRIGIDO: Substituído 'paddingHorizontal' por 'paddingStart' e 'paddingEnd' -->
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>

        <item name="cornerRadius">8dp</item>
        <item name="android:gravity">start|center_vertical</item>
        <item name="iconTint">#191932</item>
    </style>

</resources>
